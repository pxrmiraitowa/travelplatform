# 出行旅游平台测试追溯表

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 追溯范围 | REQ01—REQ14、UC01—UC14、代码模块、自动化测试、CI/CD 证据 |
| 被测分支 | GitHub `codex/microservices-ci-integration` |
| 被测提交 | `224ab89d9d087f3473d5e6cabb71a830c2f305cb` |
| 流水线 | GitHub Actions Run `33355187870` |
| 测试结论 | 203 项不同的自动化测试用例全部通过，另对部署环境复跑 16 项浏览器测试 |
| 更新日期 | 2026-08-31 |

## 2. 覆盖标记

| 标记 | 含义 |
| --- | --- |
| D | 直接覆盖：测试执行了该需求的关键业务规则或完整 API 流程 |
| P | 部分覆盖：验证页面入口、鉴权或相关子流程，未覆盖完整业务闭环 |
| — | 当前层级没有独立测试，主要由其他层级验证 |

所有标记为 D 或 P 的现有自动化用例在本次流水线中均已通过。“P”表示覆盖深度边界，不表示测试失败。

## 3. 需求—设计—代码—测试追溯矩阵

| 需求 | 业务用例 | 主要页面/接口 | 主要代码模块 | 单元测试 | 集成/API 测试 | E2E 测试 | 覆盖结论 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| REQ01 | UC01 用户注册登录与退出 | `/login`；`/api/auth/register`、`login`、`logout`；后台认证接口 | Auth、AdminAuth、JWT、Security | `AuthServiceImplTest`、`AdminAuthServiceImplTest`、`JwtTokenProviderTest`、`RegisterRequestValidationTest`（D） | `AuthIntegrationTest`（D） | 受保护场景注册测试用户并建立登录态；`E2E-TC13`、`TC14` 验证后台登录入口（P） | 直接覆盖 |
| REQ02 | UC02 个人资料与常用联系人管理 | `/profile`；`/api/users/me`、`/api/user-contacts` | User、UserContact | `UserServiceImplTest`、`UserContactServiceImplTest`、资料及联系人 DTO 校验测试（D） | `UserProfileIntegrationTest`、`UserContactIntegrationTest`（D） | `E2E-TC02 个人资料入口`（P） | 直接覆盖 |
| REQ03 | UC03 机票查询、详情查看与下单 | `/flight`；航班公开接口、`POST /api/orders/flights` | Flight、Order、Coupon、Inventory | `FlightServiceImplTest`、`OrderServiceImplTest`（D） | `OrderFlowIntegrationTest`、`PublicCatalogPriceIntegrationTest`（D） | `E2E-TC03 机票查询入口`、`E2E-SMOKE-03`（P） | 直接覆盖，浏览器下单待增强 |
| REQ04 | UC04 火车票查询、详情查看与下单 | `/train`；车次公开接口、`POST /api/orders/trains` | Train、Order、Inventory | `TrainServiceImplTest`、`OrderServiceImplTest`（D） | `OrderFlowIntegrationTest`（D） | `E2E-TC04 火车票查询入口`（P） | 直接覆盖，浏览器下单待增强 |
| REQ05 | UC05 酒店查询、详情查看与预订 | `/hotel`；酒店/房型接口、`POST /api/orders/hotels` | Hotel、HotelRoom、Order | `HotelServiceImplTest`、`OrderServiceImplTest`（D） | `OrderFlowIntegrationTest`（D） | `E2E-TC05 酒店查询入口`（P） | 直接覆盖，浏览器预订待增强 |
| REQ06 | UC06 旅游产品浏览、详情查看与下单 | `/tour`；旅游产品接口、`POST /api/orders/tours` | Tour、Order、Inventory | `TourServiceImplTest`、`OrderServiceImplTest`（D） | `OrderFlowIntegrationTest`（D） | `E2E-TC06 度假产品入口`（P） | 直接覆盖，浏览器下单待增强 |
| REQ07 | UC07 订单查询、详情查看与取消 | `/orders`；订单列表、详情、取消接口 | Order | `OrderServiceImplTest`（D） | `OrderFlowIntegrationTest`（D） | `E2E-TC07 订单入口`（P） | 直接覆盖，浏览器取消待增强 |
| REQ08 | UC08 已完成订单评价 | `/orders`；可评价订单、提交评价、评价查询接口 | Review、Order | `ReviewServiceImplTest`、`OrderServiceImplTest`（D） | `ReviewShareAdminIntegrationTest`（D） | `E2E-TC08 评价入口`（P） | 直接覆盖，浏览器提交评价待增强 |
| REQ09 | UC09 手动行程规划管理 | `/trip-plans`；行程及每日安排 CRUD 接口 | TripPlan | `TripPlanServiceImplTest`（D） | `TripPlanAuthorizationIntegrationTest`（D） | `E2E-TC09 行程规划入口`（P） | 直接覆盖，浏览器 CRUD 待增强 |
| REQ10 | UC10 AI 或本地规则生成行程并保存 | `/trip-plans`；AI 预览、保存行程接口 | AiTripPlan、TripPlan、Attraction | `AiTripPlanServiceImplTest`、`TripPlanServiceImplTest`（D） | 行程保存及鉴权由 `TripPlanAuthorizationIntegrationTest` 覆盖（P） | `E2E-TC10 AI 行程入口`（P） | 核心规则直接覆盖，完整生成保存流程待增强 |
| REQ11 | UC11 旅行分享发布与浏览 | `/shares`；分享发布、详情、列表接口 | Share、Media | `ShareServiceImplTest`、`MediaUploadServiceImplTest`（D） | `ReviewShareAdminIntegrationTest`、`ShareMediaAdminProductIntegrationTest`（D） | `E2E-TC11 分享入口`（P） | 直接覆盖，浏览器发布待增强 |
| REQ12 | UC12 价格对比与价格提醒 | `/flight`；价格比较、提醒 CRUD 接口 | PriceCompare、PriceAlert | `PriceCompareServiceImplTest`、`PriceAlertServiceImplTest`（D） | `PublicCatalogPriceIntegrationTest`、`PriceAlertIntegrationTest`（D） | `E2E-TC12 价格提醒入口`（P） | 直接覆盖，浏览器创建提醒待增强 |
| REQ13 | UC13 管理员商品与图片管理 | `/admin/products/*`；后台商品、房型、上传接口 | AdminProduct、MediaUpload | `AdminProductManageServiceImplTest`、`MediaUploadServiceImplTest`（D） | `ShareMediaAdminProductIntegrationTest`（D） | `E2E-TC13 管理员登录入口`（P） | 直接覆盖，后台页面操作待增强 |
| REQ14 | UC14 管理员用户、订单与内容管理 | `/admin/users`、`orders`、`content/*`；后台管理接口 | AdminUser、AdminOrder、AdminContent、AdminDashboard | `AdminUserManageServiceImplTest`、`AdminOrderManageServiceImplTest`、`AdminContentManageServiceImplTest`、`AdminDashboardServiceImplTest`（D） | `ReviewShareAdminIntegrationTest`、`ShareMediaAdminProductIntegrationTest`（D） | `E2E-TC14 后台权限入口`（P） | 直接覆盖，后台页面操作待增强 |

## 4. 测试层级汇总

| 测试层级 | 总数 | 通过 | 失败 | 覆盖定位 |
| --- | ---: | ---: | ---: | --- |
| 后端单元测试 | 124 | 124 | 0 | 关键类、方法、业务规则、校验与异常分支 |
| 微服务单元测试 | 13 | 13 | 0 | 微服务健康接口、库存与订单关键规则 |
| 后端集成/API 测试 | 15 | 15 | 0 | 模块调用、安全链、数据库访问和主要业务流程 |
| 前端单元测试 | 16 | 16 | 0 | API 封装、状态、路由、请求处理和组件交互 |
| Playwright E2E | 16 | 16 | 0 | 页面入口、公开路由、注册鉴权、受保护页面和后台入口 |
| 部署后公开 API 回归 | 5 | 5 | 0 | 各服务健康/版本、商品、价格、分享和认证公开接口 |
| 部署后业务回归 | 14 | 14 | 0 | UC01—UC14 通过网关完成真实数据库业务流程 |
| **不同测试用例合计** | **203** | **203** | **0** | **现有自动化测试全部通过** |

## 5. CI/CD 门禁追溯

| 门禁编号 | 输入 | 自动动作 | 通过条件 | 失败影响 | 本次结果 |
| --- | --- | --- | --- | --- | --- |
| GATE-CI-01 | 微服务分支提交 | 安装依赖并编译单体、微服务和前端 | Maven 和 Vite 构建成功 | 停止后续测试与发布 | 通过 |
| GATE-CI-02 | 编译产物 | 后端单元测试 124 项 | 失败/错误为 0 | 阻止集成测试和 CD | 通过 |
| GATE-CI-03 | 微服务代码 | 微服务单元测试 13 项 | 失败/错误为 0 | 阻止 CD | 通过 |
| GATE-CI-04 | 前端代码 | 前端单元测试 16 项 | 失败为 0 | 阻止集成测试和 CD | 通过 |
| GATE-CI-05 | 应用与 MySQL | 集成/API 测试 15 项 | 失败/错误为 0 | 阻止 E2E 和 CD | 通过 |
| GATE-CI-06 | 已启动前后端 | Playwright E2E 16 项 | 失败为 0；`pipefail` 传递失败码 | 阻止镜像构建和 CD | 通过 |
| GATE-CD-01 | 全部 CI 通过 | 构建并推送 6 个版本化镜像到 GHCR | 镜像标签与摘要生成成功 | 不进入部署 | 通过 |
| GATE-CD-02 | GHCR 版本镜像 | 部署 MySQL、4 个业务服务、网关和前端到 Kind | Pod Ready，健康、表归属及 19 项部署后回归全部正常 | 流水线失败 | 通过 |

## 6. 设计与文档追溯位置

| 追溯对象 | 文件 |
| --- | --- |
| 业务场景 | `docs/完整业务场景用例清单.md` |
| 需求与系统顺序图 | `docs/需求说明书.md`、`docs/需求说明书/req*.png` |
| 概要设计与组件顺序图 | `docs/软件概要设计说明书.md`、`docs/概要设计说明书/req*.png` |
| 详细设计与对象顺序图 | `docs/软件详细设计说明书.md`、`docs/详细设计说明书/req*.png` |
| 测试计划 | `docs/测试计划.md` |
| 测试报告 | `docs/测试报告.md` |
| 流水线 | `.github/workflows/ci-cd.yml` |

## 7. 测试证据

最新成功流水线：

```text
https://github.com/pxrmiraitowa/travelplatform/actions/runs/33355187870
```

| 证据 | 对应内容 |
| --- | --- |
| `monolith-unit-test-evidence-33355187870` | 124 项单体兼容单元测试日志和 Surefire 报告 |
| `microservices-unit-test-evidence-33355187870` | 13 项微服务单元测试日志和 Surefire 报告 |
| `frontend-unit-test-evidence-33355187870` | 16 项前端单元测试日志 |
| `monolith-integration-test-evidence-33355187870` | 15 项集成/API 测试及数据库验证报告 |
| `browser-end-to-end-test-evidence-33355187870` | 16 项 Playwright 报告、JUnit XML、截图和 Trace |
| `microservices-image-build-evidence-33355187870` | 6 个版本化镜像的标签与摘要 |
| `microservices-deployment-evidence-33355187870` | Kubernetes 状态、健康/版本、表归属、19 项部署后回归和浏览器复测 |

## 8. 结论

REQ01—REQ14 均可追溯到业务用例、设计文档、实现模块和至少一个自动化测试层级。14 项需求均具有单元、集成/API 或部署后业务回归的直接覆盖，现有 203 项不同的自动化测试用例全部通过。

浏览器 E2E 对 14 项业务用例提供页面入口和鉴权覆盖；部署后 `business-e2e.test.js` 进一步通过网关、真实微服务和四个逻辑数据库直接执行 UC01—UC14 的完整 API 业务流程。后续仍可继续增强浏览器层的表单填写和业务结果断言。
