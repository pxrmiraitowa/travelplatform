# 微服务人工业务验收记录-20260827

## 1. 验收范围

本次验收基于 `codex/microservices-split` 分支，目标是确认微服务改造后的本地端到端业务链路可演示。验收范围覆盖前端页面、gateway 路由、用户服务、产品服务、订单服务、内容行程服务以及后台管理端主要页面。

## 2. 验收环境

| 项目 | 配置 |
| --- | --- |
| 前端地址 | `http://127.0.0.1:5173` |
| gateway | `http://localhost:8000` |
| user-service | `http://localhost:8101` |
| product-service | `http://localhost:8102` |
| order-service | `http://localhost:8103` |
| content-trip-service | `http://localhost:8104` |
| 普通用户 | `demo_user / 123456` |
| 管理员 | `admin / 123456` |

## 3. 自动前置检查

| 检查项 | 结果 |
| --- | --- |
| 初始化 `travel_user`、`travel_product`、`travel_order`、`travel_content_trip` | 通过 |
| 五个微服务端口监听 | 通过 |
| `scripts/smoke-gateway.ps1` 网关冒烟测试 | 通过 |
| 前端 `npm run build` | 通过 |

## 4. 用户端验收

| 模块 | 验收内容 | 结果 |
| --- | --- | --- |
| 登录与个人中心 | 使用 `demo_user / 123456` 登录，进入个人中心，加载用户资料、常用出行人、价格提醒 | 通过 |
| 产品列表 | 机票、火车票、酒店、旅游产品列表正常加载演示数据 | 通过 |
| 产品详情 | 机票、火车票、酒店、旅游产品详情正常加载，价格比对区域可展示 | 通过 |
| 酒店/旅游详情图片 | 演示数据图片地址不可访问时显示中文占位，不再显示 Element Plus 默认 `FAILED` 文本 | 通过 |
| 订单创建 | 在机票下单页选择联系人并提交订单，订单进入统一订单中心 | 通过 |
| 订单详情 | 新订单详情可查看订单号、状态、类型、金额、联系人等信息 | 通过 |
| 订单状态流转 | 通过 gateway 调用支付和完成订单接口，前端刷新后显示已完成 | 通过 |
| 订单评价 | 已完成订单展示“去评价”，提交评价后订单详情展示评价内容，订单列表展示“已评价” | 通过 |
| 内容分享 | 旅行分享列表和分享详情可打开 | 通过 |
| 行程规划 | 行程规划页面可打开，空数据状态正常 | 通过 |

本次生成的演示订单为 `TP2026082722070571757B`，评价内容为“微服务联调验收评价：下单、完成订单与评价链路正常。”。

## 5. 后台端验收

| 模块 | 验收内容 | 结果 |
| --- | --- | --- |
| 后台登录 | 使用 `admin / 123456` 登录后台 | 通过 |
| 控制台 | 用户总数、产品总数、订单总数、近 7 天订单统计可展示 | 通过 |
| 用户管理 | 用户列表、角色、启用状态可展示 | 通过 |
| 航班管理 | 航班列表可展示 | 通过 |
| 车次管理 | 车次列表可展示 | 通过 |
| 酒店管理 | 酒店列表可展示 | 通过 |
| 房型管理 | 房型列表可展示 | 通过 |
| 旅游产品管理 | 旅游产品列表可展示 | 通过 |
| 订单管理 | 订单列表可展示，能看到本次验收创建的订单 | 通过 |
| 分享管理 | 分享列表可展示 | 通过 |
| 评价管理 | 评价列表可展示，能看到本次验收提交的评价 | 通过 |

## 6. 本次修复

| 问题 | 修复方式 |
| --- | --- |
| 订单评价提交成功后，订单详情仍显示“当前订单暂未评价” | 订单详情页加载已完成订单时补充调用 `/api/orders/{id}/review`，并合并 `reviewInfo` 与 `reviewed` 状态 |
| 订单列表中已评价订单仍显示“未评价” | 订单列表对已完成订单补充查询评价状态，前端展示与内容服务评价结果保持一致 |
| 后台首页提示“内容管理暂未启用” | 更新 dashboard 模块说明，使其匹配当前已迁移的分享/评价管理能力 |
| 酒店和旅游详情图片加载失败时出现英文 `FAILED` | 增加中文图片失败占位，改善演示观感 |

## 7. 剩余观察项

当前前端主流程和后台主页面已经可演示。后续可以继续优化后台订单、分享、评价列表中的用户展示，目前部分页面仍以 `用户#id` 展示用户信息；这不影响主流程可用性，但最终演示材料中如果追求观感，可以再补充后台侧用户基础信息聚合展示。

订单状态中的“模拟支付”和“完成订单”能力已经在 gateway 后端接口中可用，但前端订单详情页目前没有直接提供“模拟支付”按钮。本次验收通过接口完成状态流转，后续可以考虑把该按钮补回前端，便于老师或同学直接通过页面完成全流程演示。

## 8. 优惠券恢复后复验记录

本轮复验基于提交 `2325015 feat(microservices): restore optional coupon settlement` 之后的本地运行环境，重点确认优惠券作为可选增强恢复后，不影响原有微服务主链路。

### 8.1 环境状态

| 检查项 | 结果 |
| --- | --- |
| gateway `8000` | 已运行 |
| user-service `8101` | 已运行 |
| product-service `8102` | 已运行 |
| order-service `8103` | 已运行 |
| content-trip-service `8104` | 已运行 |
| 前端 Vite `5173` | 已运行 |
| `scripts/smoke-gateway.ps1` | 通过 |

### 8.2 API 验收结果

| 模块 | 验收内容 | 结果 |
| --- | --- | --- |
| 用户登录 | `demo_user / 123456` 登录，返回 token | 通过 |
| 当前用户 | `/api/users/me` 返回当前用户信息 | 通过 |
| 常用联系人 | `/api/user-contacts` 返回联系人数据 | 通过 |
| 产品查询 | 机票、酒店、旅游产品公开查询接口返回数据 | 通过 |
| 价格对比 | 机票、酒店、旅游产品价格对比接口返回优惠券列表 | 通过 |
| 不使用优惠券下单 | 航班 1 创建订单，`totalAmount=880`，`discountAmount=0` | 通过 |
| 使用适用优惠券下单 | 航班 1 使用优惠券 2 创建订单，`couponName=Flight Instant 20`，`totalAmount=860`，`discountAmount=20` | 通过 |
| 不适用优惠券拦截 | 航班订单传旅游优惠券 3，返回 `优惠券不适用于当前订单` | 通过 |
| 未达门槛优惠券拦截 | 旅游产品 1 使用优惠券 3，返回 `订单金额未达到优惠券使用门槛` | 通过 |
| 订单列表 | `/api/orders` 返回订单列表 | 通过 |
| 可评价订单 | `/api/orders/reviewable` 返回可评价订单数据 | 通过 |
| 内容分享 | `/api/public/shares` 和 `/api/shares/mine` 返回正常 | 通过 |
| 行程规划 | `/api/trip-plans` 返回正常 | 通过 |
| 价格提醒 | `/api/price-alerts` 返回正常 | 通过 |
| 后台管理 | dashboard、商品、订单、分享、用户管理接口返回正常 | 通过 |

说明：个人游记列表接口为 `GET /api/shares/mine`；`GET /api/shares` 不是列表接口，`POST /api/shares` 用于发布游记。

### 8.3 前端页面验收结果

| 页面 | 验收内容 | 结果 |
| --- | --- | --- |
| 登录页 | 普通用户登录表单正常显示，可登录 | 通过 |
| 机票预订页 | 显示优惠券选择、票面价、优惠金额、预计订单金额；机票优惠券 `Flight Instant 20` 可在下拉中看到 | 通过 |
| 酒店预订页 | 显示优惠券选择、原价、优惠金额、预计订单金额；酒店优惠券 `Hotel Save 40` 可在下拉中看到 | 通过 |
| 旅游产品预订页 | 显示优惠券选择、原价、优惠金额、预计订单金额；旅游优惠券 `Tour Save 200` 因未达门槛展示不可用提示 | 通过 |
| 订单中心 | 页面可打开，无前端控制台错误 | 通过 |
| 旅行分享 | 页面可打开，无前端控制台错误 | 通过 |
| 行程规划 | 页面可打开，无前端控制台错误 | 通过 |
| 个人中心 | 页面可打开，可看到个人资料和常用出行人区域 | 通过 |
| 后台首页 | 管理员登录后可打开后台首页 | 通过 |
| 后台商品 | 航班管理页面可打开 | 通过 |
| 后台订单 | 订单管理页面可打开 | 通过 |
| 后台内容 | 分享管理页面可打开 | 通过 |

### 8.4 本轮结论

优惠券功能已按“可选项”恢复：不选择优惠券不影响原有下单主链路，选择适用优惠券可正确结算优惠金额，选择不适用或未达门槛优惠券会被服务端拒绝。微服务 gateway 主链路、前端关键页面和后台关键页面在本轮复验中均可演示。
