# 改造前后版本与提交记录

## 1. 仓库和分支

- GitHub：`https://github.com/pxrmiraitowa/travelplatform`
- 最终微服务交付分支：`codex/microservices-ci-integration`
- 完整提交记录以 Git 仓库对象为准，不使用手工整理的截图代替。

## 2. 改造前原系统

- 标签：`monolith-start`
- 提交：`9c4ae950d623c2c9ca77e8ef20e25ca1581c2671`
- 提交时间：2026-06-09 23:14:40 +08:00
- 用途：作为微服务改造前的单体版本基线，用于功能和性能对照。

## 3. 微服务版本

- 中期标签：`midterm-2026-08-29-microservices`
- 标签指向提交：`5bbc3bb2ec62e482ed820c16d7705b65830173cb`
- 正式实验业务源码：`aece11988e2364289625c0de2c75b18444c55b8d`
- 成员 E 完成交付提交：`99899543a9b8460f185bb22d7403bd875fbc84b9`
- HPA 复现修复提交：`50bf4f6515a8ac90943cbad16480259ea498b64c`
- 原始证据与可编辑报告入库提交：`07ce4c12e871b1b24ea13bf5190e13b67bb6cd56`
- 2026-09-02 合并检查点：`cc6c5eea85e21c18cca5cf8d7736be3b7d11611e`

`9989954`、`50bf4f6` 和 `07ce4c1` 只补充实验配置、结果、复现保护和交付材料，没有改变三个业务源码目录，因此 2026-08-31 的正式镜像和实验结果仍对应 `aece119`。

`cc6c5ee` 合并后，三个业务源码目录相对 `aece119` 只有 `travel-platform-web/vite.config.js` 一行变化：Vite 本地开发服务器的默认代理从 `localhost:8000` 改为单体联调使用的 `localhost:8080`。该设置只作用于 `npm run dev`；Docker/Kubernetes 的生产前端由 Nginx 转发请求，不使用 Vite 开发代理，因此不改变正式实验镜像中的业务代码、接口、数据和 Nginx 路由。实验脚本只接受这一项内容及规范化 SHA-256 完全匹配的已审查差异；出现任何其他业务源码变化都会拒绝复用旧基线并要求重新构建、重跑实验。

## 4. 验证命令

```powershell
git fetch origin --tags
git show --no-patch monolith-start
git show --no-patch midterm-2026-08-29-microservices
git log --graph --decorate --oneline --all
git log --reverse --format="%H`t%ci`t%an`t%s" > 完整提交记录.tsv
git diff --name-status aece119..HEAD -- travel-platform-server travel-platform-microservices travel-platform-web
pwsh -NoProfile -File scripts/verify-member-e-delivery.ps1
```

远程仓库应同时存在 `monolith-start` 和 `midterm-2026-08-29-microservices` 两个标签。导出的 TSV 只是便于教师查看的快照，完整、不可丢失的记录仍是 Git 历史本身。
