# 出行旅游平台测试报告

## 1. 报告摘要

| 项目 | 内容 |
| --- | --- |
| 测试对象 | 出行旅游平台 `travelplatform` |
| 被测分支 | GitHub `codex/microservices-ci-integration` |
| 被测提交 | `224ab89d9d087f3473d5e6cabb71a830c2f305cb` |
| 流水线 | GitHub Actions 第 31 次运行（Run ID：`33355187870`） |
| 执行日期 | 2026-08-31 |
| 综合结论 | **通过，满足当前流水线发布条件** |

本轮对单体兼容单元测试、微服务单元测试、后端集成/API 测试、前端单元测试、Playwright 浏览器测试、部署后公开 API 测试、14 个业务场景回归以及 CI/CD 部署验证进行了复核。共包含 **203 项不同的自动化测试用例**，全部通过；其中 16 项 Playwright 用例在微服务部署完成后再次执行并通过。

最新一次微服务流水线中的 6 个 CI 阶段和 2 个 CD 阶段全部成功。CI 完成单体兼容、微服务、前端、集成/API 和浏览器测试；CD 构建并发布 4 个业务服务、网关和前端共 6 个镜像，并在 Kind Kubernetes 集群中完成部署、健康检查、数据表归属检查和部署后回归。

## 2. 测试范围

- 后端关键业务规则、请求校验、安全处理和异常分支的单元测试。
- 认证、用户、订单、价格提醒、评价、分享、后台管理、行程规划等集成/API 测试。
- 前端 API、状态管理、路由守卫、请求拦截、登录页面和价格比较组件测试。
- Playwright 页面入口、公开路由、鉴权路由和后台登录入口测试。
- GitHub Actions 编译、测试、镜像构建、发布、Kubernetes 部署和健康检查。
- MySQL、后端、前端三个 Docker/Kubernetes 工作负载的部署验证。

## 3. 测试环境

| 组件 | CI 配置 |
| --- | --- |
| 执行平台 | GitHub Actions `ubuntu-latest` |
| 后端运行时 | JDK 17、Maven |
| 前端运行时 | Node.js 22、npm |
| 后端测试 | JUnit 5、Mockito、AssertJ、Spring Boot Test、MockMvc |
| 前端测试 | Vitest 4.1.8 |
| E2E 测试 | Playwright 1.61.0、Chromium |
| 数据库 | MySQL 8.4 |
| 镜像与部署 | Docker Buildx、GHCR、Kind v0.32.0、Kubernetes |

本地使用 Windows 11、JDK 17、Maven 3.9.11 和 Node.js 对后端、前端测试数量及结果进行了复核。

## 4. 测试结果汇总

| 测试类型 | 测试类/文件 | 总数 | 通过 | 失败 | 错误 | 跳过 | 通过率 | 结论 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 后端单元测试 | 27 类 | 124 | 124 | 0 | 0 | 0 | 100% | 通过 |
| 微服务单元测试 | 6 类 | 13 | 13 | 0 | 0 | 0 | 100% | 通过 |
| 后端集成/API 测试 | 9 个测试类，另有 1 个公共基类 | 15 | 15 | 0 | 0 | 0 | 100% | 通过 |
| 前端单元测试 | 6 文件 | 16 | 16 | 0 | 0 | 0 | 100% | 通过 |
| Playwright E2E | 1 个正常测试文件 | 16 | 16 | 0 | 0 | 0 | 100% | 通过 |
| 部署后公开 API 回归 | 5 组 | 5 | 5 | 0 | 0 | 0 | 100% | 通过 |
| 部署后业务场景回归 | UC01—UC14 | 14 | 14 | 0 | 0 | 0 | 100% | 通过 |
| **不同测试用例合计** |  | **203** | **203** | **0** | **0** | **0** | **100%** | **通过** |


## 5. 后端单元测试

执行命令：

```powershell
cd travel-platform-server
mvn -B "-Dtest=!*IntegrationTest" test
```

执行结果：

```text
Tests run: 124, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

主要覆盖注册登录、JWT、用户资料、联系人、产品查询、订单创建、库存和优惠券规则、评价、分享、价格提醒、行程规划、后台管理、参数校验、媒体上传和全局异常处理。此前订单测试中业务码与校验顺序不一致的问题已经修复，最新 `OrderServiceImplTest` 11 项全部通过。

## 6. 后端集成/API 测试

执行命令：

```powershell
cd travel-platform-server
mvn -B "-Dtest=*IntegrationTest" test
```

集成测试使用 `@SpringBootTest`、MockMvc、真实安全链和 MySQL 8.4，对 HTTP 状态、统一响应结构、业务字段和数据库状态进行断言。

15 项测试全部通过，覆盖注册登录、用户资料、联系人、四类订单创建、订单查询与取消、库存恢复、公共产品目录、价格比较、价格提醒、行程规划、评价、分享、后台商品和媒体上传。

## 7. 前端单元测试

测试文件：

```text
travel-platform-web/src/api/auth.test.js
travel-platform-web/src/components/PriceComparePanel.test.js
travel-platform-web/src/router/index.test.js
travel-platform-web/src/stores/user.test.js
travel-platform-web/src/utils/request.test.js
travel-platform-web/src/views/auth/LoginView.test.js
```

执行命令与结果：

```powershell
cd travel-platform-web
npm ci
npm run test:unit
```

```text
Test Files  6 passed (6)
Tests       16 passed (16)
```

测试覆盖认证接口、用户状态持久化、路由权限、请求拦截与 401 处理、登录页面交互和价格比较组件。

## 8. Playwright 端到端测试

执行命令：

```powershell
cd travel-platform-web
npm run test:e2e
```

执行结果：

```text
16 passed
0 failed
```

正常回归覆盖首页、机票、火车票、酒店、度假、旅行分享、个人中心、订单、行程规划、管理员登录和后台权限入口。鉴权场景通过真实注册接口创建独立测试用户，写入有效 Token 和用户信息后访问受保护路由。

Playwright 已配置 HTML 报告、JUnit XML、失败截图、失败 Trace 和 CI 重试。流水线启用 `set -o pipefail`，任一 E2E 失败都会正确标红并阻止 CD。

## 9. CI/CD 流水线结果

工作流：`.github/workflows/ci-cd.yml`

```text
Run ID: 33355187870
Run number: 31
Commit: 224ab89d9d087f3473d5e6cabb71a830c2f305cb
Conclusion: success
```

| 阶段 | 内容 | 结果 |
| --- | --- | --- |
| CI 1/6 | 检出、依赖安装、单体/微服务/前端编译 | 成功 |
| CI 2/6 | 单体兼容单元测试 | 成功 |
| CI 3/6 | 微服务单元测试 | 成功 |
| CI 4/6 | 前端单元测试 | 成功 |
| CI 5/6 | 单体集成/API 测试 | 成功 |
| CI 6/6 | Playwright 浏览器测试 | 成功 |
| CD 1/2 | 构建并发布 6 个版本化镜像到 GHCR | 成功 |
| CD 2/2 | 部署微服务并执行健康、表归属、API、业务和浏览器回归 | 成功 |

只有全部 CI 测试成功后才会进入 CD。发布镜像使用不可变的 `sha-<提交前12位>` 标签和 `0.1.<流水线序号>` 标签，不使用 `latest`。

## 10. Docker 与 Kubernetes 验证

| 服务 | 作用 | 验证内容 |
| --- | --- | --- |
| `mysql` | MySQL 8.4 数据库 | StatefulSet 就绪、初始化完成 |
| `user-service` | 用户、认证与联系人 | Deployment 就绪、健康与版本接口正常 |
| `product-service` | 商品、价格与库存 | Deployment 就绪、健康与版本接口正常 |
| `order-service` | 订单与状态流转 | Deployment 就绪、健康与版本接口正常 |
| `content-trip-service` | 行程、分享、评价与提醒 | Deployment 就绪、健康与版本接口正常 |
| `gateway-service` | 统一入口和 JWT 校验 | Deployment 就绪、健康与版本接口正常 |
| `frontend` | Vue/Nginx 前端 | Deployment 就绪、`/healthz` 返回 `ok` |

部署顺序：

```text
MySQL 初始化并就绪 → 四个业务服务发布 → 网关发布 → 前端发布 → 健康、表归属和业务回归
```

第 31 次流水线已完成 6 个版本化镜像的拉取与 Kind 加载、Kubernetes 部署、Pod Ready 等待、各服务健康和版本检查、数据库表归属检查、19 项部署后 API/业务回归以及 16 项部署后浏览器复测。Kind 位于临时 GitHub Actions runner，任务结束后销毁，不是长期在线生产环境。

## 11. 缺陷复测与关闭

| 编号 | 原问题 | 复测结果 | 状态 |
| --- | --- | --- | --- |
| TEST-001 | 订单单元测试期望 400、实际 404 | 后端单元测试 124/124 通过 | 已关闭 |
| TEST-002 | 固定旅游日期过期导致集成测试错误 | 集成/API 测试 15/15 通过 | 已关闭 |
| TEST-003 | E2E 未执行 | Playwright 16/16 通过 | 已关闭 |
| TEST-004 | CI 未设置 E2E 发布门禁 | 已加入 CI 6/6，失败阻止 CD | 已关闭 |
| TEST-005 | E2E 失败被 `tee` 吞掉 | 已启用 `set -o pipefail` | 已关闭 |
| TEST-006 | 未验证容器运行状态 | Kubernetes 部署和健康检查成功 | 已关闭 |

## 12. 遗留风险与建议

本轮没有阻断发布的测试缺陷，后续建议：

1. 增加至少一条完整浏览器业务闭环：登录、查询产品、创建订单、查询订单、取消订单。
2. 为长期开发或生产 Kubernetes 集群增加持续在线的部署目标。
3. 定期执行依赖审计、镜像漏洞扫描和密钥泄露扫描。
4. 按课程后续要求增加覆盖率阈值。

## 13. 发布判定

本次测试结论为：**通过，允许进入当前 CD 发布与部署验证阶段**。

判定依据：

- 后端单元测试 124/124 通过。
- 后端集成/API 测试 15/15 通过。
- 前端单元测试 16/16 通过。
- Playwright E2E 测试 16/16 通过。
- 203 项不同的自动化测试用例全部通过。
- 6 个微服务和前端镜像构建及 GHCR 发布成功。
- MySQL、4 个业务服务、网关和前端在 Kubernetes 中部署成功。
- 健康、版本、数据表归属和部署后业务回归全部成功。

## 14. 测试证据

流水线地址：

```text
https://github.com/pxrmiraitowa/travelplatform/actions/runs/33355187870
```

流水线构件：

- `microservices-build-output-33355187870`
- `monolith-unit-test-evidence-33355187870`
- `microservices-unit-test-evidence-33355187870`
- `frontend-unit-test-evidence-33355187870`
- `monolith-integration-test-evidence-33355187870`
- `browser-end-to-end-test-evidence-33355187870`
- `microservices-image-build-evidence-33355187870`
- `microservices-deployment-evidence-33355187870`

构件包含 Maven Surefire XML、Playwright HTML/JUnit 报告、测试日志、镜像版本记录、Kubernetes 部署日志、容器状态和健康检查结果，可用于现场检查和追溯。
