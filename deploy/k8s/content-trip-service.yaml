apiVersion: apps/v1
kind: Deployment
metadata:
  name: content-trip-service
  namespace: travel-platform
  labels:
    app.kubernetes.io/name: content-trip-service
    app.kubernetes.io/part-of: travel-platform-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: content-trip-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: content-trip-service
        app.kubernetes.io/part-of: travel-platform-microservices
    spec:
      containers:
        - name: content-trip-service
          image: travel-platform-content-trip-service:0.1.0
          imagePullPolicy: IfNotPresent
          env:
            - name: DB_URL
              value: jdbc:mysql://mysql:3306/travel_content_trip?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
            - name: DB_USERNAME
              value: root
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: travel-platform-secrets
                  key: mysql-root-password
            - name: USER_SERVICE_URL
              value: http://user-service:8101
            - name: PRODUCT_SERVICE_URL
              value: http://product-service:8102
            - name: ORDER_SERVICE_URL
              value: http://order-service:8103
            - name: UPLOAD_DIR
              value: /app/uploads
            - name: SERVER_PORT
              value: "8104"
            - name: TZ
              value: Asia/Shanghai
          ports:
            - name: http
              containerPort: 8104
          volumeMounts:
            - name: content-uploads
              mountPath: /app/uploads
          startupProbe:
            httpGet:
              path: /api/public/health
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 48
          readinessProbe:
            httpGet:
              path: /api/public/health
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /api/public/health
              port: http
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 5
      volumes:
        - name: content-uploads
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: content-trip-service
  namespace: travel-platform
  labels:
    app.kubernetes.io/name: content-trip-service
    app.kubernetes.io/part-of: travel-platform-microservices
spec:
  selector:
    app.kubernetes.io/name: content-trip-service
  ports:
    - name: http
      port: 8104
      targetPort: http
