apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: travel-platform
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/part-of: travel-platform-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: order-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: order-service
        app.kubernetes.io/part-of: travel-platform-microservices
    spec:
      containers:
        - name: order-service
          image: travel-platform-order-service:0.1.0
          imagePullPolicy: IfNotPresent
          env:
            - name: DB_URL
              value: jdbc:mysql://mysql:3306/travel_order?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
            - name: DB_USERNAME
              value: root
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: travel-platform-secrets
                  key: mysql-root-password
            - name: PRODUCT_SERVICE_URL
              value: http://product-service:8102
            - name: SERVER_PORT
              value: "8103"
            - name: TZ
              value: Asia/Shanghai
          ports:
            - name: http
              containerPort: 8103
          startupProbe:
            httpGet:
              path: /api/public/health
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 48
          readinessProbe:
            httpGet:
              path: /api/public/health
              port: http
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /api/public/health
              port: http
            periodSeconds: 15
            timeoutSeconds: 3
            failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: travel-platform
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/part-of: travel-platform-microservices
spec:
  selector:
    app.kubernetes.io/name: order-service
  ports:
    - name: http
      port: 8103
      targetPort: http
