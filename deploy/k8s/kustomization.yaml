# Reuse the upstream CI manifests; the member-e overlay supplies its own namespace.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - mysql.yaml
  - db-init-job.yaml
  - user-service.yaml
  - product-service.yaml
  - order-service.yaml
  - content-trip-service.yaml
  - gateway-service.yaml
  - frontend.yaml
