# Structure example only. Do not apply this file: deployment scripts create the
# real Secret with independent random values and never write those values here.
apiVersion: v1
kind: Secret
metadata:
  name: travel-platform-secrets
  namespace: travel-platform-micro-team
  annotations:
    lab.travelplatform/template-only: "true"
type: Opaque
stringData:
  mysql-root-password: REPLACE_AT_DEPLOY_TIME
  jwt-secret: REPLACE_AT_DEPLOY_TIME
