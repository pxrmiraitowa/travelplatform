# 成员 E 本地实验配置层

在不修改组员业务代码和原 CI 清单的前提下，准备 `travel-platform-micro-team` 独立区域、三个 PVC、资源限制及商品服务 HPA。当前入口固定到 2026-08-31 远程最新版 `aece11988e2364289625c0de2c75b18444c55b8d` 的本地版本化镜像；标签同时包含提交短号和 JAR/前端源码摘要。

从仓库根目录执行 `pwsh -NoProfile -File scripts/prepare-member-e-kind.ps1` 只生成配置和预检结果，不执行部署或 SQL。不要直接把全部清单应用到现有环境。

`secret.example.yaml` 只说明 Secret 的键名和结构，不能直接应用。实际部署脚本在内存中生成独立随机值并通过 Kubernetes API 创建 Secret，证据文件不保存密码或 JWT 密钥。

本配置层与 `deploy/k8s` 基础目录同级；请使用此入口，不要递归应用整个 deploy 目录。应用镜像的 `imagePullPolicy` 为 `Never`，部署前必须先运行最新版构建脚本并把对应镜像导入 Kind；这样节点缺少正确镜像时会明确失败，不会从网络拉取同名替代品。当前方案限单节点 amd64 Kind，PVC 不等于跨节点共享或备份。

文件说明、验证记录与实际部署的后续步骤见[新仓库接入说明](../../docs/成员E-新仓库接入与部署准备-20260828.md)。

2026-08-31 已在 `aece119` 上重新完成单体/微服务构建、独立 Kubernetes 部署、HPA 1→5→1、依赖故障验证，以及两种架构各 3 轮的正式性能对比。最终结果、证据索引和答辩解释见[成员 E 第一二阶段完成报告](../../docs/成员E-第一二阶段完成报告-20260831.md)。以后源码版本前进时，必须重新构建、替换六个内容版本标签并重跑实验，不能把本次结果套到新代码上。
